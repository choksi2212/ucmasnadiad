# Dependencies
node_modules/
.pnpm-store/

# Next.js build output
.next/
out/

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# OS files
.DS_Store
Thumbs.db

# Editor directories
.vscode/
.idea/
*.swp
*.swo
# Local photo sources (optional on disk after `npm run upload:media` → Cloudinary).
# Safe to omit from git if all assets are uploaded and env has Cloudinary on deploy.
RESULTS/
STAFF/
LIFE/
public/photos/
public/STAFF/
public/RESULTS/
public/LIFE/
# Optional extra local icons (tab icon is tracked `public/ucmas-logo.svg`; override path in `layout.tsx` if you use another file).
public/favicon.ico
public/favicon.png
public/vidd.webm
public/vidd.mov

# Next.js generated files
next-env.d.ts
*.tsbuildinfo

# Package manager lock files (keep one)
yarn.lock
pnpm-lock.yaml
.vercel
